---
target: homepage transfer UI post-fix re-run
total_score: 28
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 3
timestamp: 2026-07-29T06-29-08Z
slug: src-main-jsx
---
# Critique re-run — src/main.jsx (post-fix, trend measurement)

Method: dual-agent (A: ReviewA2 · B: DetectorB2)
Scope: default Base→Arbitrum first paint, dest chain sheet, desktop 1440 + mobile 390, light theme.

## Heuristic Scores (28/40 — Good; prior runs 27 → 25)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | CTA label ladder; pre-connect Fee/Balance "—" thin |
| 2 | Match System / Real World | 3 | Self-claim / Orbit / SVM / "dedicated RPC" still insider lexicon |
| 3 | User Control and Freedom | 3 | Esc/backdrop close, swap, disconnect, resume; no free undo in-flight |
| 4 | Consistency and Standards | 3 | Tokenized themes; aria double-word fixed this run |
| 5 | Error Prevention | 3 | Serveable default route; Solana still selectable then blocked |
| 6 | Recognition Rather Than Recall | 3 | Popularity-ordered chain sheet; no search/recents |
| 7 | Flexibility and Efficiency | 2 | No typeahead/shortcuts for 23-chain list |
| 8 | Aesthetic and Minimalist Design | 3 | Amount hero + wider shadowed card; dense secondary copy stack |
| 9 | Error Recovery | 3 | Resume/retry/friendlyError strength intact |
| 10 | Help and Documentation | 2 | External docs; no in-context plain-language guide |

## 大气 (scale/presence) assessment

Fixed: amount display hero (44px/600/tabular-nums) with demoted h1; card 860px + stronger light shadow; live enabled CTA on first paint; no RPC wall in default corridor. Still small: empty "0.0" faint ghost state; mobile amount 30.4px; dense secondary copy stack between recipient and CTA.

## Specificity

Moderately product-specific: operate-true (fixed USDC, Self-claim/Orbit, fee honesty, resume) still on a category-shaped card skeleton; character is policy/status honesty, not yet a signature visual.

## Cognitive load: 2/8 failures (was 5/8)

Remaining: one-thing-at-a-time (completion+Fast+facts+trust all pre-commit) and minimal-choices (23-chain sheet without search).

## Priority Issues

- **[P1]** 23-chain sheet is a wall — popularity helps; needs search / recents / Popular-vs-More grouping (`$impeccable distill` on chain picker).
- **[P1]** Empty amount underplays the new hero — idle treatment: darker placeholder / caret-ready emphasis (`$impeccable bolder` on amount panel).
- **[P1]** Secondary copy stack dilutes the CTA band — one mint explanation, one-clause trust line (`$impeccable quieter`).
- **[P2]** Solana selectable then blocked — prevention half-done; consider disabling with explanation (`$impeccable harden`).
- **[P2]** Mobile hero regresses 大气 — hold amount ≥1.75–2rem at 390px (`$impeccable adapt`).

## Detector (B)

116 findings (−2 vs 118): font-size 73 / color 26 / radius 17. New amount hero (2.75rem) and card title (1.25rem) steps are on the documented ramp and pass. False positives carried forward: 23 CHAIN_META brand colors; 7 hits on prose-documented 10px radii. Browser overlay skipped by design (page CSP `script-src 'self'`); CLI + preflight as fallback.

## Persona red flags

Jordan: Self-claim/Orbit jargon uncoached; empty recipient; "needs dedicated RPC" scary without guidance; Fee/Balance "—" reads broken. Alex: no chain typeahead or shortcuts; forced linear flow; Solana selectable-then-blocked is sloppy constraint UX.

## Minor observations (fixed this run)

Aria double-word "Choose source chain chain" → fixed. DESIGN.md drift (disabled opacity 0.4, old shadow token) → synced to live CSS.
